[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio      = { workspace = true }
scada-core = { path = "../scada-core" }
modbus     = { path = "../modbus" }
simulator  = { path = "../simulator" }
tracing = {workspace =  true}